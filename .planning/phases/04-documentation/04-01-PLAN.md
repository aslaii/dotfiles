---
phase: 04-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-04
must_haves:
  truths:
    - "A developer can clone the repo and run the bootstrap without reading source code"
    - "A developer can look up any symlink mapping without reading emit_symlink_map()"
    - "A developer can discover all environment flags and their defaults"
    - "A developer can understand the skills system and create a new skill by following written instructions"
    - "A developer can find the Neovim plugin upgrade workflow without reading nvim/AGENTS.md"
  artifacts:
    - path: "README.md"
      provides: "Root documentation with bootstrap, symlink map, env flags, tool overview, skills system, drift check, Neovim workflow"
      contains: "## Quick Start"
  key_links:
    - from: "README.md"
      to: "initial-setup-macos.sh"
      via: "Quick Start code block"
      pattern: "initial-setup-macos\\.sh"
    - from: "README.md"
      to: "emit_symlink_map()"
      via: "Source of truth note in symlink map section"
      pattern: "emit_symlink_map"
    - from: "README.md"
      to: "skills/fullstack-bridge/SKILL.md"
      via: "Canonical example reference in skills section"
      pattern: "fullstack-bridge"
---

<objective>
Create the root README.md for the dotfiles repository covering bootstrap instructions, full symlink map, environment flags reference, tool overview, skills system documentation, drift check usage, and Neovim plugin workflow.

Purpose: A developer (including future self on a new machine) can understand the full setup, run it, and extend the AI skills system without reading any source code first.
Output: `README.md` at repo root
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentation/04-CONTEXT.md
@.planning/phases/04-documentation/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root README.md with bootstrap, symlink map, env flags, and tool overview</name>
  <files>README.md</files>
  <read_first>
    - initial-setup-macos.sh (read emit_symlink_map() function around lines 198-226, env flag defaults around lines 3-9, --check mode around line 630)
    - macos/zsh/zsh/aliases.zsh (read dotcheck alias around line 9)
    - CLAUDE.md (read Directory Map table for cross-reference, do NOT duplicate)
    - AGENTS.md (read for cross-reference conventions)
    - .planning/phases/04-documentation/04-RESEARCH.md (source material inventory)
  </read_first>
  <action>
Create `README.md` at repo root with the following sections, per D-01 (single comprehensive file) and D-02 (required sections):

**1. Header:**
- Title: `# Dotfiles`
- One-line description: "Personal macOS dotfiles — one command to full productivity."

**2. Quick Start** (per D-02):
```markdown
## Quick Start

```bash
# Clone the repo
git clone https://github.com/aslaii/dotfiles.git ~/dotfiles

# Run the bootstrap (idempotent — safe to rerun)
bash ~/dotfiles/initial-setup-macos.sh

# Verify symlinks
dotcheck
# or: bash ~/dotfiles/initial-setup-macos.sh --check
```
```

**3. Symlink Map** (per D-02, D-04):
Section header: `## Symlink Map`
Add blockquote: `> Source of truth: \`emit_symlink_map()\` in \`initial-setup-macos.sh\``

Create a 3-column table (`Repo Path | Symlink Target | Tool`) with EXACTLY these rows from `emit_symlink_map()` (per D-04, must match exactly):

| Repo Path | Symlink Target | Tool |
|-----------|----------------|------|
| `macos/zsh/zshrc` | `~/.zshrc` | Zsh config |
| `macos/zsh/zsh/` | `~/.zsh` | Zsh scripts |
| `tmux/tmux.conf` | `~/.tmux.conf` | tmux |
| `codex/AGENTS.md` | `~/AGENTS.md` | Codex CLI |
| `ghostty/config` | `~/Library/Application Support/com.mitchellh.ghostty/config` | Ghostty |
| `codex/` | `~/.config/codex` | Codex CLI |
| `yabai/` | `~/.config/yabai` | yabai |
| `skhd/` | `~/.config/skhd` | skhd |
| `opencode/` | `~/.config/opencode` | OpenCode |
| `claude/` | `~/.claude` | Claude Code |
| `sketchybar/` | `~/.config/sketchybar` | SketchyBar * |
| `nvim/` | `~/.config/nvim` | Neovim / LazyVim |

Add footnote: `* SketchyBar symlink is conditional on \`SKETCHYBAR=true\` (default).`

IMPORTANT: Do NOT include `gemini/` in this table — it exists in the repo but is NOT in `emit_symlink_map()`. Verify by reading the actual function.

**4. Environment Flags** (per D-02):
Section header: `## Environment Flags`

| Flag | Default | Purpose |
|------|---------|---------|
| `DOTFILES_DIR` | `$HOME/dotfiles` | Location of the cloned dotfiles repo |
| `CONFIG_VARIANT` | `macos` | Selects which zshrc variant to link |
| `SKETCHYBAR` | `true` | Whether to install SketchyBar and create its symlink |

Add usage example: `SKETCHYBAR=false bash ~/dotfiles/initial-setup-macos.sh`

**5. Tool Overview** (per D-02):
Section header: `## Tools`
Brief description of each managed tool (one line each):
- **Neovim** — LazyVim-based config with lazy-lock.json for reproducible plugins
- **tmux** — Terminal multiplexer config
- **Ghostty** — GPU-accelerated terminal emulator config
- **yabai** — Tiling window manager (see `yabai/README.md` for setup)
- **skhd** — Hotkey daemon for yabai keybindings
- **SketchyBar** — Custom macOS menu bar (optional)
- **starship** — Cross-shell prompt
- **Zinit** — Zsh plugin manager with Turbo mode
- **Claude Code** — AI assistant config and skills
- **Gemini CLI** — AI assistant config
- **OpenCode** — AI assistant config
- **Codex CLI** — AI assistant config

**6. Drift Check** (per D-02):
Section header: `## Drift Check`
Document two ways to check symlink drift:
- `dotcheck` — shell alias available in any Zsh session
- `bash ~/dotfiles/initial-setup-macos.sh --check` — validates all symlinks without modifying anything, exits non-zero if drift detected

**7. Cross-References** section:
- `CLAUDE.md` — AI assistant configuration and project conventions
- `AGENTS.md` — Shell script conventions and commit style
- `yabai/README.md` — yabai setup (SIP, sudoers, keybindings)
- `setups/README.md` — Role-script system documentation

Per D-03, this is static markdown — no auto-generation scripts.
  </action>
  <verify>
    <automated>grep -c "## Quick Start" README.md && grep -c "## Symlink Map" README.md && grep -c "## Environment Flags" README.md && grep -c "## Tools" README.md && grep -c "## Drift Check" README.md && grep -c "emit_symlink_map" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at repo root
    - README.md contains `## Quick Start` with `initial-setup-macos.sh` command
    - README.md contains `## Symlink Map` with `emit_symlink_map()` source-of-truth note
    - README.md symlink table has exactly 12 rows (matching emit_symlink_map output)
    - README.md does NOT contain `gemini/` in the symlink table
    - README.md contains `## Environment Flags` with DOTFILES_DIR, CONFIG_VARIANT, SKETCHYBAR
    - README.md contains `## Tools` section
    - README.md contains `## Drift Check` with `dotcheck` and `--check`
    - README.md contains cross-references to CLAUDE.md, AGENTS.md, yabai/README.md, setups/README.md
  </acceptance_criteria>
  <done>Root README.md has Quick Start, Symlink Map (matching emit_symlink_map exactly), Environment Flags, Tool Overview, and Drift Check sections</done>
</task>

<task type="auto">
  <name>Task 2: Add Skills System and Neovim Plugin Workflow sections to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (must read Task 1 output before modifying)
    - skills/fullstack-bridge/SKILL.md (canonical skill example per D-12)
    - claude/skills/react-native/SKILL.md (Claude-specific skill example)
    - claude/skills/pr-standards/SKILL.md (Claude-specific skill example)
    - nvim/AGENTS.md (Neovim plugin workflow source)
  </read_first>
  <action>
Append two sections to the existing README.md (after the Tools section but before Cross-References):

**1. Skills System** (per D-11, D-12, D-13):
Section header: `## Skills System`

Opening paragraph: "Skills provide structured guidance for AI coding assistants. Each skill is a self-contained directory with a `SKILL.md` index and a `references/` subdirectory containing detailed rules."

Subsection `### Directory Structure`:
```
skills/              # Repo-global skills (any AI assistant)
claude/skills/       # Claude Code-specific skills
```

Subsection `### Available Skills`:
Table listing all skills:

| Skill | Location | Description |
|-------|----------|-------------|
| `fullstack-bridge` | `skills/fullstack-bridge/` | NestJS + React/RN synchronization |
| `react-native` | `claude/skills/react-native/` | Expo Router, TanStack Query, Zustand patterns |
| `pr-standards` | `claude/skills/pr-standards/` | PR size limits, branch naming, review rules |

Subsection `### Invoking Skills` (per D-13):
"In Claude Code, use `/skill <name>` to activate a skill. Claude reads the `SKILL.md` file and loads relevant rules from `references/` as needed."

Subsection `### Creating a New Skill` (per D-12):
Step-by-step instructions:
1. Create directory: `skills/<name>/` (repo-global) or `claude/skills/<name>/` (Claude-specific)
2. Create `SKILL.md` with YAML frontmatter:
   ```yaml
   ---
   name: my-skill
   description: When to use this skill.
   ---
   ```
3. Add detailed rules in `<name>/references/` subdirectory
4. Reference `skills/fullstack-bridge/SKILL.md` as the canonical example

**2. Neovim Plugin Workflow** (per D-14, satisfies NVIM-02 deferral):
Section header: `## Neovim Plugin Workflow`

Document the upgrade process:
1. Open Neovim and run `:Lazy update`
2. Commit `nvim/lazy-lock.json` with the plugin changes
3. On a new machine, `:Lazy sync` restores pinned versions
4. Run `:checkhealth` to verify plugin health

Add note: "`nvim/lazy-lock.json` is tracked in git to ensure reproducible plugin installs across machines."
  </action>
  <verify>
    <automated>grep -c "## Skills System" README.md && grep -c "fullstack-bridge" README.md && grep -c "## Neovim Plugin Workflow" README.md && grep -c ":Lazy update" README.md && grep -c "/skill" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `## Skills System` section
    - README.md contains `### Creating a New Skill` subsection
    - README.md contains `fullstack-bridge` as canonical example reference (per D-12)
    - README.md contains `/skill <name>` invocation documentation (per D-13)
    - README.md contains `skills/` and `claude/skills/` directory structure (per D-11)
    - README.md contains `## Neovim Plugin Workflow` section with `:Lazy update` and `lazy-lock.json` (per D-14)
    - README.md contains `### Invoking Skills` subsection
  </acceptance_criteria>
  <done>Skills system fully documented with directory structure, available skills, invocation, and creation instructions. Neovim plugin workflow documented with upgrade process and reproducibility note.</done>
</task>

</tasks>

<verification>
- `grep -c "## Quick Start\|## Symlink Map\|## Environment Flags\|## Tools\|## Drift Check\|## Skills System\|## Neovim Plugin Workflow" README.md` returns 7 (all required sections present)
- `grep "emit_symlink_map" README.md` returns the source-of-truth note
- `grep "fullstack-bridge" README.md` returns the canonical example reference
- `grep "gemini/" README.md` does NOT appear in the symlink table (gemini is not in emit_symlink_map)
- `grep ":Lazy update" README.md` returns the Neovim workflow instruction
</verification>

<success_criteria>
Root README.md is a complete, self-contained document that a developer can follow to bootstrap a new machine, understand every symlink, discover environment flags, check for drift, navigate the skills system, and manage Neovim plugins — all without reading source code.
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation/04-01-SUMMARY.md`
</output>
