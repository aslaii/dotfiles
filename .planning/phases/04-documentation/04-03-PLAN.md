---
phase: 04-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - yabai/README.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04

must_haves:
  truths:
    - "No dead links exist in any documentation file in the repo"
    - "ROADMAP.md success criteria for Phase 4 reference only tools that exist in the repo"
    - "REQUIREMENTS.md DOC-02 reflects the actual state of the repo (yabai removed)"
    - "The yabai/ directory no longer exists in the repo"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 4 success criteria without yabai reference"
      contains: "Success Criteria"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated DOC-02 marking yabai docs as removed/superseded"
      contains: "DOC-02"
  key_links: []
---

<objective>
Remove stale yabai documentation and update planning artifacts to reflect that yabai/skhd/sketchybar were removed from the repo (commit 57f47e3).

Purpose: Close the single verification gap from 04-VERIFICATION.md -- yabai/README.md references deleted files (yabairc, ../skhd/skhdrc) and the yabai/ directory should not exist since the tool is no longer managed by this dotfiles repo.

Output: yabai/ directory removed, ROADMAP.md and REQUIREMENTS.md updated to remove yabai references from Phase 4 criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-documentation/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove yabai directory and update planning artifacts</name>
  <files>yabai/README.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - yabai/README.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/04-documentation/04-VERIFICATION.md
  </read_first>
  <action>
    1. Delete the entire `yabai/` directory (it only contains README.md):
       ```
       rm -rf yabai/
       ```

    2. Update `.planning/ROADMAP.md` Phase 4 Success Criteria:
       - Remove criterion #2 which currently reads: `yabai/README.md documents the SIP disable procedure and scripting-addition sudoers entry needed for yabai to function`
       - Renumber remaining criteria (current #3 becomes #2, current #4 becomes #3)
       - The updated success criteria should be:
         1. `README.md` at repo root documents one-command bootstrap, full symlink map, env flag reference, and skills system overview
         2. `setups/` scripts are documented with the shared `functions.sh` contract so adding a new role script requires no reverse-engineering
         3. The AI skills system convention is documented so a new skill can be added by following the written instructions alone

    3. Update `.planning/REQUIREMENTS.md` DOC-02 entry:
       - Change from: `- [x] **DOC-02**: yabai/README.md with SIP disable procedure and scripting-addition sudoers entry`
       - Change to: `- [x] **DOC-02**: ~~yabai/README.md~~ Removed -- yabai/skhd/sketchybar no longer in repo (commit 57f47e3). Requirement superseded.`
  </action>
  <verify>
    <automated>test ! -d yabai/ && ! grep -q 'yabai/README.md' .planning/ROADMAP.md && grep -q 'superseded' .planning/REQUIREMENTS.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `yabai/` directory does not exist: `test ! -d yabai/`
    - ROADMAP.md Phase 4 success criteria has exactly 3 items, none mentioning yabai: `grep -c 'yabai' .planning/ROADMAP.md` returns 0 for the success criteria section
    - REQUIREMENTS.md DOC-02 line contains "superseded" and strikethrough: `grep 'DOC-02' .planning/REQUIREMENTS.md | grep -q 'superseded'`
    - No dead links remain in any tracked markdown: `grep -rn 'yabairc\|skhdrc\|skhd/' $(git ls-files '*.md') | grep -v VERIFICATION | grep -v CONTEXT | grep -v SUMMARY | grep -v PLAN | grep -v DISCUSSION` returns no results
  </acceptance_criteria>
  <done>yabai/ directory removed. ROADMAP.md success criteria updated to 3 items without yabai reference. REQUIREMENTS.md DOC-02 marked as superseded.</done>
</task>

</tasks>

<verification>
- `test ! -d yabai/` -- yabai directory is gone
- `grep -c 'yabai/README.md' .planning/ROADMAP.md` in the Success Criteria section returns 0
- `grep 'DOC-02' .planning/REQUIREMENTS.md` shows superseded status
- No dead links to yabairc or skhdrc in any non-planning markdown file
</verification>

<success_criteria>
1. The yabai/ directory no longer exists in the repository
2. ROADMAP.md Phase 4 has 3 success criteria, none referencing yabai
3. REQUIREMENTS.md DOC-02 is marked as superseded
4. Zero dead links to removed files (yabairc, skhdrc, skhd/) in tracked documentation
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation/04-03-SUMMARY.md`
</output>
