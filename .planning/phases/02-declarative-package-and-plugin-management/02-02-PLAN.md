---
phase: 02-declarative-package-and-plugin-management
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - initial-setup-macos.sh
  - Brewfile.lock.json
autonomous: true
requirements:
  - PKG-02
  - PKG-04
  - NVIM-02

must_haves:
  truths:
    - "The bootstrap script installs all packages via brew bundle install from the Brewfile"
    - "No inline BREW_TAPS, BREW_FORMULAE, or BREW_CASKS arrays remain in the setup script"
    - "The SKETCHYBAR toggle still works -- setting SKETCHYBAR=false skips sketchybar installation"
    - "Brewfile.lock.json is committed to the repo"
    - "The Neovim plugin upgrade workflow is documented in nvim/AGENTS.md"
  artifacts:
    - path: "initial-setup-macos.sh"
      provides: "Migrated bootstrap using brew bundle"
      contains: "brew bundle install"
    - path: "Brewfile.lock.json"
      provides: "Homebrew debug lockfile (advisory only)"
  key_links:
    - from: "initial-setup-macos.sh"
      to: "Brewfile"
      via: "brew bundle install --file="
      pattern: "brew bundle install.*Brewfile"
    - from: "initial-setup-macos.sh"
      to: "HOMEBREW_BUNDLE_BREW_SKIP"
      via: "export when SKETCHYBAR is false"
      pattern: "HOMEBREW_BUNDLE_BREW_SKIP"
---

<objective>
Migrate the bootstrap script from inline package arrays to `brew bundle install`, commit the Brewfile lockfile, and verify the Neovim plugin workflow documentation.

Purpose: Completes the declarative package management migration. After this plan, `initial-setup-macos.sh` uses the Brewfile as its single source of truth and no parallel package lists exist.
Output: Rewritten `ensure_homebrew_packages()` function, removed arrays and old functions, committed `Brewfile.lock.json`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-declarative-package-and-plugin-management/02-CONTEXT.md
@.planning/phases/02-declarative-package-and-plugin-management/02-RESEARCH.md
@.planning/phases/02-declarative-package-and-plugin-management/02-01-SUMMARY.md

@initial-setup-macos.sh
@Brewfile
@nvim/AGENTS.md

<interfaces>
<!-- Current main() call order in initial-setup-macos.sh (lines 645-675) -->
main() {
  require_macos
  ensure_dotfiles_dir
  ensure_command_line_tools
  install_homebrew
  ensure_brew_shellenv
  ensure_brew_taps          # REMOVE -- replaced by brew bundle
  install_formulae          # REMOVE -- replaced by brew bundle
  install_pre_commit_hooks  # KEEP -- stays after ensure_homebrew_packages
  install_casks             # REMOVE -- replaced by brew bundle
  ensure_batcat_symlink
  install_clasp
  ...
}

<!-- Functions to REMOVE entirely -->
ensure_brew_taps()   -- lines 177-189
install_formulae()   -- lines 191-211
install_casks()      -- lines 213-225

<!-- Arrays to REMOVE entirely -->
BREW_TAPS    -- lines 11-15
BREW_FORMULAE -- lines 17-49
BREW_CASKS   -- lines 51-60

<!-- Keep unchanged -->
SKETCHYBAR="${SKETCHYBAR:-true}"  -- line 9
is_truthy() function
log(), warn(), die() helpers

<!-- nvim/AGENTS.md already documents lazy-lock.json workflow -->
Line 8: "lazy-lock.json captures resolved plugin commits; regenerate via Lazy after plugin changes."
Line 28: "Include lazy-lock.json updates in the same commit as the spec change..."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate bootstrap script to brew bundle</name>
  <files>initial-setup-macos.sh</files>
  <read_first>initial-setup-macos.sh, Brewfile</read_first>
  <action>
Modify `initial-setup-macos.sh` with these specific changes:

**1. Remove inline arrays (lines 11-60).**
Delete the `BREW_TAPS`, `BREW_FORMULAE`, and `BREW_CASKS` array declarations entirely. Per D-02, the Brewfile is the single source of truth.

Keep these lines at the top (currently lines 5-9):
```bash
DOTFILES_DIR="${DOTFILES_DIR:-$HOME/dotfiles}"
CONFIG_VARIANT="${CONFIG_VARIANT:-macos}"
BREW_BIN=""
BREW_PREFIX=""
SKETCHYBAR="${SKETCHYBAR:-true}"
```

**2. Remove old functions.**
Delete these three functions entirely:
- `ensure_brew_taps()` (currently lines 177-189)
- `install_formulae()` (currently lines 191-211)
- `install_casks()` (currently lines 213-225)

**3. Add new `ensure_homebrew_packages()` function.**
Place it where `ensure_brew_taps()` used to be (after `ensure_brew_shellenv`). Per D-05, D-06, D-07, D-08:

```bash
ensure_homebrew_packages() {
  local brewfile="$DOTFILES_DIR/Brewfile"

  if [[ ! -f "$brewfile" ]]; then
    die "Brewfile not found at $brewfile"
  fi

  # Conditionally skip sketchybar packages (per D-08)
  if ! is_truthy "$SKETCHYBAR"; then
    log "Skipping SketchyBar packages (SKETCHYBAR=${SKETCHYBAR})."
    export HOMEBREW_BUNDLE_BREW_SKIP="sketchybar"
    export HOMEBREW_BUNDLE_TAP_SKIP="FelixKratz/formulae"
  fi

  # Check if all packages already installed (per D-07)
  if brew bundle check --file="$brewfile" 2>/dev/null; then
    log "All Homebrew packages already installed."
    return
  fi

  log "Installing Homebrew packages from Brewfile..."
  if ! brew bundle install --file="$brewfile" --no-upgrade; then
    warn "Some Brewfile packages may have failed. Check output above."
  fi
}
```

**4. Update `main()` function.**
Replace the three old calls with one new call. The current main() has:
```
  ensure_brew_taps
  install_formulae
  install_pre_commit_hooks
  install_casks
```

Replace with:
```
  ensure_homebrew_packages
  install_pre_commit_hooks
```

`install_pre_commit_hooks` stays in its current position (after package installation). All other main() calls remain unchanged.

**5. Remove deprecated tap cleanup.**
The old `homebrew/cask-fonts` tap was in `BREW_TAPS` which is now deleted. Optionally add a one-time cleanup line inside `ensure_homebrew_packages()` before the bundle check:
```bash
  # Clean up deprecated tap if still present
  brew untap homebrew/cask-fonts 2>/dev/null || true
```
Place this after the SKETCHYBAR skip block and before the `brew bundle check` call. Per Pitfall 4, the `|| true` prevents script abort.
  </action>
  <verify>
    <automated>grep -q 'brew bundle install' initial-setup-macos.sh && echo "has brew bundle install" && grep -q 'brew bundle check' initial-setup-macos.sh && echo "has brew bundle check" && grep -q 'HOMEBREW_BUNDLE_BREW_SKIP' initial-setup-macos.sh && echo "has SKIP env" && ! grep -q 'BREW_TAPS=' initial-setup-macos.sh && echo "no BREW_TAPS" && ! grep -q 'BREW_FORMULAE=' initial-setup-macos.sh && echo "no BREW_FORMULAE" && ! grep -q 'BREW_CASKS=' initial-setup-macos.sh && echo "no BREW_CASKS" && ! grep -q 'ensure_brew_taps' initial-setup-macos.sh && echo "no ensure_brew_taps" && ! grep -q 'install_formulae' initial-setup-macos.sh && echo "no install_formulae" && ! grep -q 'install_casks' initial-setup-macos.sh && echo "no install_casks" && grep -q 'ensure_homebrew_packages' initial-setup-macos.sh && echo "has ensure_homebrew_packages" && grep -q 'install_pre_commit_hooks' initial-setup-macos.sh && echo "kept install_pre_commit_hooks" && bash -n initial-setup-macos.sh && echo "syntax valid"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'BREW_TAPS=' initial-setup-macos.sh` returns no match (array removed)
    - `grep 'BREW_FORMULAE=' initial-setup-macos.sh` returns no match (array removed)
    - `grep 'BREW_CASKS=' initial-setup-macos.sh` returns no match (array removed)
    - `grep 'ensure_brew_taps' initial-setup-macos.sh` returns no match (function removed)
    - `grep 'install_formulae' initial-setup-macos.sh` returns no match (function removed)
    - `grep 'install_casks' initial-setup-macos.sh` returns no match (function removed)
    - `grep 'brew bundle install.*Brewfile' initial-setup-macos.sh` returns a match
    - `grep 'brew bundle check.*Brewfile' initial-setup-macos.sh` returns a match
    - `grep 'HOMEBREW_BUNDLE_BREW_SKIP.*sketchybar' initial-setup-macos.sh` returns a match
    - `grep 'HOMEBREW_BUNDLE_TAP_SKIP.*FelixKratz' initial-setup-macos.sh` returns a match
    - `grep 'ensure_homebrew_packages' initial-setup-macos.sh` returns a match in main()
    - `grep 'install_pre_commit_hooks' initial-setup-macos.sh` returns a match in main() (preserved)
    - `grep 'SKETCHYBAR=.*true' initial-setup-macos.sh` returns a match (env var preserved)
    - `grep 'is_truthy' initial-setup-macos.sh` returns a match (function preserved)
    - `bash -n initial-setup-macos.sh` exits 0 (valid syntax)
    - `grep 'homebrew/cask-fonts' initial-setup-macos.sh` returns a match in the untap cleanup line only (with `|| true`)
  </acceptance_criteria>
  <done>Bootstrap script uses brew bundle install from Brewfile. All inline arrays and old install functions removed. SKETCHYBAR toggle works via HOMEBREW_BUNDLE_BREW_SKIP. install_pre_commit_hooks preserved in correct position. Script passes bash -n syntax check.</done>
</task>

<task type="auto">
  <name>Task 2: Generate Brewfile.lock.json and verify Neovim lockfile</name>
  <files>Brewfile.lock.json</files>
  <read_first>Brewfile, nvim/AGENTS.md, nvim/lazy-lock.json</read_first>
  <action>
**1. Generate Brewfile.lock.json (per D-09, D-10).**
Run `brew bundle install --file=Brewfile` on the current machine. This generates `Brewfile.lock.json` alongside the Brewfile. The lockfile is a debug artifact, not a version pin (per research Pitfall 1), but is committed per user decision D-09.

If `brew bundle install` fails for some packages, that is acceptable -- the lockfile is still generated for the packages that succeeded. Use `--no-upgrade` flag to avoid upgrading already-installed packages.

Command: `brew bundle install --file="$PWD/Brewfile" --no-upgrade`

After generation, verify `Brewfile.lock.json` exists and is non-empty.

**2. Verify nvim/lazy-lock.json (per D-11).**
Confirm:
- `nvim/lazy-lock.json` exists and is valid JSON (use `python3 -m json.tool nvim/lazy-lock.json > /dev/null`)
- `git ls-files nvim/lazy-lock.json` shows it is tracked
- `.gitignore` no longer contains `lazy-lock` (should have been fixed in Plan 01)

**3. Verify NVIM-02 documentation (per D-12, D-13).**
Read `nvim/AGENTS.md` and confirm it already documents:
- Line 8: `lazy-lock.json captures resolved plugin commits; regenerate via Lazy after plugin changes.`
- Line 28: `Include lazy-lock.json updates in the same commit as the spec change...`

This satisfies D-12 (the workflow is documented). Per D-13, full user-facing documentation belongs in Phase 4. No changes needed to nvim/AGENTS.md if both lines are present.

If either line is missing, add them per the format already established in nvim/AGENTS.md.
  </action>
  <verify>
    <automated>test -s Brewfile.lock.json && echo "lockfile exists and non-empty" && python3 -m json.tool nvim/lazy-lock.json > /dev/null 2>&1 && echo "lazy-lock.json is valid JSON" && git ls-files nvim/lazy-lock.json | grep -q 'lazy-lock' && echo "lazy-lock tracked in git" && ! grep -q 'lazy-lock' .gitignore && echo "not in gitignore" && grep -q 'lazy-lock.json' nvim/AGENTS.md && echo "documented in AGENTS.md"</automated>
  </verify>
  <acceptance_criteria>
    - `test -s Brewfile.lock.json` exits 0 (file exists and is non-empty)
    - `python3 -m json.tool Brewfile.lock.json > /dev/null 2>&1` exits 0 (valid JSON)
    - `python3 -m json.tool nvim/lazy-lock.json > /dev/null 2>&1` exits 0 (valid JSON)
    - `git ls-files nvim/lazy-lock.json` outputs `nvim/lazy-lock.json`
    - `grep 'lazy-lock' .gitignore` returns no match
    - `grep 'lazy-lock.json' nvim/AGENTS.md` returns at least 2 matches (workflow documentation)
  </acceptance_criteria>
  <done>Brewfile.lock.json is generated and committed. nvim/lazy-lock.json is verified as tracked, valid JSON, and not gitignored. Neovim plugin upgrade workflow is documented in nvim/AGENTS.md.</done>
</task>

</tasks>

<verification>
- `brew bundle check --file=Brewfile 2>/dev/null` passes (or reports only SKETCHYBAR-related skips)
- `grep -c 'BREW_TAPS\|BREW_FORMULAE\|BREW_CASKS' initial-setup-macos.sh` returns 0
- `grep 'brew bundle install' initial-setup-macos.sh` confirms migration
- `bash -n initial-setup-macos.sh` passes syntax check
- `Brewfile.lock.json` exists and is valid JSON
- `nvim/lazy-lock.json` tracked and valid
</verification>

<success_criteria>
- Bootstrap script uses `brew bundle install` from Brewfile -- no inline arrays remain
- SKETCHYBAR toggle preserved via HOMEBREW_BUNDLE_BREW_SKIP env var
- Brewfile.lock.json committed to repo
- nvim/lazy-lock.json verified tracked and documented
- Script passes bash syntax validation
</success_criteria>

<output>
After completion, create `.planning/phases/02-declarative-package-and-plugin-management/02-02-SUMMARY.md`
</output>
