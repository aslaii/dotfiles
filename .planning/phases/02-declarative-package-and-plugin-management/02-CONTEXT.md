# Phase 2: Declarative Package and Plugin Management - Context

**Gathered:** 2026-03-30
**Status:** Ready for planning

<domain>
## Phase Boundary

Commit a Brewfile and Neovim lockfile so new-machine setup is fully reproducible without manual intervention. The bootstrap script migrates from inline package arrays to `brew bundle install`. Deprecated taps are removed. After this phase, `brew bundle check` passes and a fresh machine gets the exact same packages.

</domain>

<decisions>
## Implementation Decisions

### Brewfile Generation
- **D-01:** Generate Brewfile programmatically from existing `BREW_TAPS`, `BREW_FORMULAE`, and `BREW_CASKS` arrays in `initial-setup-macos.sh`, then hand-verify for correctness and remove duplicates.
- **D-02:** Remove inline arrays (`BREW_TAPS`, `BREW_FORMULAE`, `BREW_CASKS`) entirely after Brewfile is committed. The Brewfile becomes the single source of truth for all Homebrew packages — no parallel lists.
- **D-03:** Remove the deprecated `homebrew/cask-fonts` tap. Cask fonts are now in Homebrew core (no tap needed). The `font-*` entries move to `cask` directives in the Brewfile without a tap prefix.
- **D-04:** Keep `koekeishiya/formulae` (yabai, skhd) and `FelixKratz/formulae` (sketchybar) as `tap` directives in the Brewfile — these are still required for third-party formulae.

### Bootstrap Script Migration
- **D-05:** Replace the individual tap/install/cask loops in `ensure_homebrew_packages()` with a single `brew bundle install --file="$DOTFILES_DIR/Brewfile"` call. This handles taps, formulae, and casks in one idempotent command.
- **D-06:** Keep the `ensure_homebrew_packages()` function name and call site — only replace its body. This preserves the script's existing structure and log messages.
- **D-07:** Add a `brew bundle check --file="$DOTFILES_DIR/Brewfile"` call before install to skip the step if everything is already installed (idempotency optimization).
- **D-08:** The `SKETCHYBAR` env var toggle currently gates sketchybar package installation. Preserve this behavior by conditionally adding/removing sketchybar from the bundle command or using `brew bundle` with `--no-lock` and a conditional approach.

### Brewfile.lock.json
- **D-09:** Commit `Brewfile.lock.json` to the repo for version reproducibility. This is generated by `brew bundle install` and captures exact installed versions.
- **D-10:** The lockfile should be regenerated on each `brew bundle install` run — no manual maintenance needed.

### Neovim Plugin Management
- **D-11:** `nvim/lazy-lock.json` is already tracked in git (NVIM-01 is effectively satisfied). Verify it's not gitignored and contains valid content.
- **D-12:** Document the Neovim plugin upgrade workflow: run `:Lazy update` in Neovim, then commit the updated `lazy-lock.json`. This satisfies NVIM-02.
- **D-13:** The documentation for NVIM-02 belongs in Phase 4 (Documentation) as a section in the Neovim docs, but the lockfile tracking verification belongs here.

### Claude's Discretion
- Exact Brewfile formatting and comment style
- Whether to alphabetize entries in the Brewfile or group by category
- How to handle the `SKETCHYBAR` toggle cleanly with `brew bundle` (conditional tap/formula inclusion)
- Whether `Brewfile.lock.json` needs a `.gitattributes` entry for diff handling

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Bootstrap Script
- `initial-setup-macos.sh` — Primary bootstrap; contains `BREW_TAPS`, `BREW_FORMULAE`, `BREW_CASKS` arrays and `ensure_homebrew_packages()` function (lines 11-230)

### Neovim Configuration
- `nvim/lazy-lock.json` — Already tracked lockfile; verify not gitignored
- `nvim/AGENTS.md` — Documents lazy-lock.json conventions and commit workflow

### Project Standards
- `AGENTS.md` — Shell script conventions, commit style
- `CLAUDE.md` §Recommended Stack — Homebrew 4.x section confirms `brew bundle` is built-in, documents `homebrew/cask-fonts` deprecation

### Prior Phase Context
- `.planning/phases/01-security-and-quality-hardening/01-CONTEXT.md` — D-13 notes shfmt added to BREW_FORMULAE with plan to move to Brewfile in Phase 2

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `ensure_homebrew_packages()` function in `initial-setup-macos.sh` — Current implementation loops through arrays; will be replaced with `brew bundle` call
- `log()`, `warn()`, `die()` helper functions — Available for Brewfile migration logging

### Established Patterns
- `SKETCHYBAR="${SKETCHYBAR:-true}"` env var toggle — Must be preserved for conditional sketchybar installation
- `set -euo pipefail` — Error handling pattern already established
- `DOTFILES_DIR="${DOTFILES_DIR:-$HOME/dotfiles}"` — Standard path derivation

### Integration Points
- `ensure_homebrew_packages()` call site in `main()` — Where the migration happens
- `nvim/lazy-lock.json` — Already tracked, connects to Neovim plugin ecosystem
- `BREW_TAPS` array at line 11 — Contains deprecated `homebrew/cask-fonts` to remove
- `BREW_FORMULAE` array at line 17 — Source of truth for formulae extraction to Brewfile
- `BREW_CASKS` array at line 51 — Source of truth for cask extraction to Brewfile

</code_context>

<specifics>
## Specific Ideas

No specific requirements — open to standard approaches. The Brewfile should follow standard Homebrew Bundle conventions.

</specifics>

<deferred>
## Deferred Ideas

- NVIM-02 documentation (plugin upgrade workflow) — belongs in Phase 4 (Documentation), only lockfile verification is Phase 2 scope
- `brew update && brew upgrade` automation wrapper — deferred to v2 (APKG-01)
- yabai/skhd version pinning — deferred to v2 (APKG-02)

</deferred>

---

*Phase: 02-declarative-package-and-plugin-management*
*Context gathered: 2026-03-30*
