---
phase: 01-security-and-quality-hardening
plan: 02
subsystem: infra
tags: [envsubst, gitignore, templates, bootstrap, security]

# Dependency graph
requires: []
provides:
  - "Template+generated config pattern for claude, gemini, codex"
  - "Gitignore coverage for all AI tool runtime artifacts"
  - "process_json_templates() bootstrap function for envsubst expansion"
  - "install_pre_commit_hooks() bootstrap function"
  - "shfmt, pre-commit, gitleaks added to BREW_FORMULAE"
affects: [01-security-and-quality-hardening, 02-package-lockfiles-and-nvim-sync]

# Tech tracking
tech-stack:
  added: [envsubst, shfmt, pre-commit, gitleaks]
  patterns: [template-generated-config-pair, scoped-envsubst]

key-files:
  created:
    - claude/settings.template.json
    - gemini/settings.template.json
    - codex/config.template.toml
    - gemini/.gitignore
    - codex/.gitignore
  modified:
    - claude/.gitignore
    - opencode/.gitignore
    - initial-setup-macos.sh

key-decisions:
  - "Scoped envsubst '$HOME' instead of bare envsubst to prevent unintended variable expansion"
  - "Codex template excludes runtime project trust entries; those are added by Codex at runtime"

patterns-established:
  - "Template+generated pattern: .template.ext is tracked, .ext is gitignored and generated by envsubst"
  - "Bootstrap function ordering: link_configs -> process_json_templates -> ensure_neovim_config"

requirements-completed: [SEC-01, SEC-02, SEC-03]

# Metrics
duration: 2min
completed: 2026-03-30
---

# Phase 01 Plan 02: Config Template+Gitignore Hardening Summary

**Replaced hardcoded /Users/aslaii paths with $HOME templates, added comprehensive gitignores for all AI tool dirs, wired envsubst into bootstrap**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-30T14:51:38Z
- **Completed:** 2026-03-30T14:54:28Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments
- All three AI tool configs (claude, gemini, codex) now use template+generated pattern with $HOME placeholders
- Comprehensive gitignore files prevent committing OAuth tokens, runtime state, and generated configs
- Bootstrap script generates machine-specific configs from templates via scoped envsubst
- Added shfmt, pre-commit, gitleaks to Homebrew formulae for quality tooling

## Task Commits

Each task was committed atomically:

1. **Task 1: Create template files and update gitignores** - `837d3d9` (feat)
2. **Task 2: Expand opencode gitignore and wire bootstrap functions** - `35afb47` (feat)

## Files Created/Modified
- `claude/settings.template.json` - Template with $HOME for claude hooks
- `gemini/settings.template.json` - Template with $HOME for gemini hooks and MCP filesystem arg
- `codex/config.template.toml` - Template with $HOME for codex hook, no project trust entries
- `gemini/.gitignore` - Covers OAuth tokens, runtime state, generated settings
- `codex/.gitignore` - Excludes generated config.toml
- `claude/.gitignore` - Added settings.json exclusion
- `opencode/.gitignore` - Added cache/, gsd-file-manifest.json, oh-my-opencode.json
- `initial-setup-macos.sh` - Added process_json_templates(), install_pre_commit_hooks(), 3 new formulae

## Decisions Made
- Used scoped `envsubst '$HOME'` instead of bare envsubst to prevent unintended variable expansion in JSON/TOML values
- Excluded codex project trust entries from template since they are machine-specific runtime state
- Placed process_json_templates() call after link_configs in main() so symlinks exist before templates are processed

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Template+generated pattern established; future configs can follow same approach
- Pre-commit and gitleaks in BREW_FORMULAE ready for plan 01-03 (pre-commit config) and 01-04 (gitleaks)
- Bootstrap script syntax-checked and ready for testing on next machine setup

---
*Phase: 01-security-and-quality-hardening*
*Completed: 2026-03-30*
